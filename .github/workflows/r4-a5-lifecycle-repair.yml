name: R4 A5 lifecycle repair

on:
  push:
    branches:
      - research/donor-foundation-repair-a5-lifecycle-2026-09-11
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup exact Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2
      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund
      - name: Qualify A5 support lifecycle repair
        run: node scripts/r4-a5-support-lifecycle-repair-crucible.mjs
      - name: Canonical smoke
        run: npm run smoke
      - name: Build runtime
        run: npm run build
