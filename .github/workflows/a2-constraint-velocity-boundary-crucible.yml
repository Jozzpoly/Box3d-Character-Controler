name: A2 constraint-velocity boundary crucible

on:
  push:
    branches:
      - research/donor-foundation-falsification-2026-09-10
    paths:
      - scripts/a2-constraint-velocity-boundary-crucible.mjs
      - .github/workflows/a2-constraint-velocity-boundary-crucible.yml
      - src/constraint-velocity.js
      - src/constraint-velocity-character.js
      - src/donor/**
      - src/character.js
      - package.json
      - package-lock.json
  workflow_dispatch:

permissions:
  contents: read

jobs:
  characterize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup exact Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2
      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund
      - name: Run A2 characterization
        run: node scripts/a2-constraint-velocity-boundary-crucible.mjs
      - name: Upload A2 evidence
        uses: actions/upload-artifact@v4
        with:
          name: a2-constraint-velocity-boundary-evidence
          path: tmp/a2/results.json
          retention-days: 90
