name: Verify and deploy playground

on:
  push:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Smoke embodied-player mechanics
        run: npm run smoke
      - name: Capture E14 continuous diagnostics
        if: github.ref != 'refs/heads/main'
        run: node scripts/e14-1d-continuous-diagnostics.mjs e14-1d-diagnostics.json
      - name: Upload E14 continuous diagnostics
        if: github.ref != 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: e14-continuous-diagnostics
          path: e14-1d-diagnostics.json
          if-no-files-found: error
      - name: Build playground
        run: npm run build
      - name: Configure Pages
        if: github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: verify
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
